global:
  metadata:
    organization: "test"
  connectivity:
    baseDomain: example.com
    containerRegistries:
      with-auth.example.com:
      - endpoint: with-auth.example.com
        credentials:
          username: giantswarmpull
          password: abcdef
      - endpoint: quay.io
