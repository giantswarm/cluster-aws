# Without the filesystem the kubelet would run against an empty directory on the EBS lib volume and
# report a node that has none of its previous pods. Fail the kubelet instead. This file only reaches
# node pools that use the instance store.
[Unit]
RequiresMountsFor=/var/lib/kubelet
